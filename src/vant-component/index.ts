/* eslint-disable */
import { Button, NavBar, Icon, List, Cell ,Uploader,Image as VanImage ,Field ,TreeSelect } from "vant";

export default function (app: any) {
  app.use(Button).use(NavBar).use(Icon).use(List).use(Cell).use(Uploader).use(VanImage).use(Field).use(TreeSelect)
}
